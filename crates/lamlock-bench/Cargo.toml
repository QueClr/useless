[package]
name = "lamlock-bench"
version = "0.1.0"
edition = "2024"

[dependencies]
lamlock = { path = "../lamlock", features = ["nightly"] }
rand = "0.9"
rand_xoshiro = "0.7"

[dev-dependencies]
criterion = "0.6.0"

[[bench]]
name = "bench"
harness = false
